# GitHub Actions workflow to build a Java Maven project and publish the artifact for CI
# This workflow runs on pushes and pull requests to the master branch
# The built JAR artifact will be uploaded to GitHub Actions for download

name: Java Maven CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
    # Step 1: Checkout repository
    - name: Checkout code
      uses: actions/checkout@v4

    # Step 2: Set up JDK 11
    - name: Set up Java 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'

    # Step 3: Build the project with Maven
    - name: Build with Maven
      run: mvn clean package --batch-mode

    # Step 4: Upload the built JAR as a workflow artifact
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: java-app
        path: target/*.jar
